# Mathematical, citation, and presentation closure

No dataset, 661x18 scenario family, selected list, optimization result, table value, or scientific conclusion was changed in this closure.

1. **Breakpoint endpoint semantics.** Reported g* values are transition boundaries. Because best rank uses a strict comparison and worst rank a non-strict comparison, the post-contraction state is applied for g < g* rather than asserted at equality.
2. **Reparameterization symbol.** Retained support width remains g; the optional homeomorphism is phi, with g=phi(alpha), phi(0)=1, and phi(1)=0.
3. **Theorem 1.** Guaranteed items are ordered by nonincreasing lower bound before insertion. This prevents a previously inserted guaranteed item from being selected as the exchange-out item h. Subsequent exclusion of Z items is by strict scenario-wise dominance, and finiteness excludes cycling.
4. **Modifier construction.** With frozen normalized exposure a_i^(c), frozen normalized five-skill O*NET values z_im, and retained skill set M_w, the paper now states h_i^(w)=mean_{m in M_w} z_im, q_i^(c,w)=a_i^(c)(1-h_i^(w)), and p_i^t=s_i q_i^(c,w). Equal-five uses all five skills; each of five ablations removes one skill.
5. **Exact box regret.** For X={i:x_i=1}, the corner expression is V_k(v(X)) - sum_{i in X} ell_i, avoiding Top-k identity ambiguity under ties.
6. **Relative minimax notation.** V_t*=V_k(p^t), capture C_t(x), relative regret R_t(x)=1-C_t(x), and 0<=rho<=1 are explicit.
7. **Nested support.** ell_i<=c_i<=u_i is stated because the point core is one primitive scenario.
8. **Absolute minimax criterion.** R_t^abs(x)=V_t* - sum_i p_i^t x_i is explicitly defined; the 23/25 overlap is identified relative to the relative-minimax list.
9. **Citations.** The Main now has 28/28 used references numbered in exact first-appearance IEEE order; the Supplement has 5/5 used references. Algorithm-engineering/solver references with no present textual role were removed. SMAA and budgeted-uncertainty references are retained because they are explicitly contrasted in the practical-interpretation section.
10. **SIPP.** The secondary SIPP diagnostic was removed because its fixed 17-occupation target construction was not sufficiently self-contained for this compact paper and was not needed for the central contribution.
11. **Stable occupations.** All 14 coherent-stable occupations are listed with SOC codes in the Supplement, with representative names surfaced in the Main.
12. **Figure production.** Plot titles, axes, tick labels, legends, and annotations are bold. Four-sided frames are visible, and annotation positions are kept inside the plotting rectangle; the previously edge-adjacent Scale only and g*=0.078756 labels are no longer clipped.
